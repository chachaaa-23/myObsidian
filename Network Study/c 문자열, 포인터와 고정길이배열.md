[[>Computer network]]
구조체의 내부에서 문자열을 포인터(`unsigned char *text`)
가 아닌 고정 길이 배열(`unsigned char text[12]`)로 선언해야 함. 

-> 구조체 안에 실제 데이터가 연속해서 들어있기 때문에 통신으로 던지기 위한 첫 번째 관문 통과. 
(만약 포인터였다면 메모리 주소값만 전송되어 수신측에서는 쓰레기값을 읽게 됩니다.)

